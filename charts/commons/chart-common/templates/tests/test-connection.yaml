apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-common.fullname" . }}-test-connection"
  labels:
    {{- include "chart-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
